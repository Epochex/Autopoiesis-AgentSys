# RCA 当前完整进度

更新时间：2026-09-03 UTC

这份文件是手机端讨论入口。先读本文件，再按文末路径检查实现。当前提交只发布进度说明；下述实现代码仍位于部署机器的未提交工作区，尚未进入 GitHub `main`。因此，GitHub 上可以讨论机制、边界和验证结果，暂时不能仅凭远端源码复现全部新行为。

## 一句话结论

当前 RCA 已拆成六类数据对象。实时状态走设备 API 或只读命令，历史事件走 ClickHouse 精确时间窗口，资产身份走确定性映射，官方知识走版本过滤与 BM25，历史故障记忆走结构化过滤与质量门控，单次调查上下文走带来源和时间的证据账本。BGE、FAISS、Torch 当前没有进入线上请求链路。

## 当前数据对象与处理方式

| 数据对象 | 主要输入 | 当前处理方式 | 查询或索引 | 主要输出 |
|---|---|---|---|---|
| 设备当前状态 | 防火墙 API、主机只读命令 | 请求时直接采集，记录观测时间 | IP、主机名、服务名精确匹配 | 当前会话、接口、路由、服务状态 |
| 日志和历史事件 | 持续摄取的网络事实 | 按设备和精确起止时间查询 | ClickHouse 排序键、时间过滤、跳数索引、Projection | 次数、先后顺序、首次和末次、聚合趋势 |
| 资产身份和拓扑 | IP、MAC、主机名、设备清单 | 确定性解析与关系限定 | 结构化映射和关联关系 | 查询主体、设备范围、关联节点 |
| 官方产品知识 | 厂商官方手册 | 按在线设备型号和版本自动同步 | 版本过滤 + BM25 | 配置步骤、命令解释、排障方法 |
| 历史故障记忆 | 症状、证据、根因、处理结果 | 结构化保存，质量门控后复用 | 实体、时间、状态过滤；语义检索保留为可选能力 | 可复用故障经验和已验证处理结果 |
| 单次调查上下文 | 本次查询命中的全部证据 | 证据 ID、来源、观测时间、结果状态统一编排 | 动态选择和有界压缩 | 可追溯的模型输入和最终解释 |

RAG 的适用范围是官方知识和历史故障记忆。实时状态、日志、资产关系各自使用原生查询能力。

## 已经实现并在部署机器验证的能力

### 1. 设备识别与官方知识自动同步

- 从在线 FortiGate 自动读取产品、型号和固件版本。
- 当前识别结果为 FortiGate FG100E、FortiOS 6.2.7。
- 自动定位并同步对应版本的 Fortinet 官方管理手册。
- 已发布 921 个章节、6384 个知识切片，文件约 12 MB，抓取覆盖率 100%。
- 同步过程采用流式解析，避免把整站 HTML 长期保存在内存。
- 新版本只有达到最低覆盖率才会原子发布。
- 同步失败时继续服务上一代知识库，并由后台任务重试。
- 知识状态接口：`GET /api/rca/knowledge-status`。

当前限制：自动知识提供器只完整实现了 Fortinet。资产清单共 456 台设备，本次盘点中 319 台缺少可靠厂商字段，347 台缺少可靠型号字段。其他厂商需要先补设备指纹，再增加受控的官方文档提供器。

### 2. 按问题意图选择数据源

自然语言首先经过确定性解析，提取 IPv4、systemd 服务名和“最近 30 分钟”等时间范围，然后生成查询计划：

| 问题 | 数据源计划 |
|---|---|
| `192.168.1.23 当前状态怎么样` | `direct_state` |
| `192.168.1.23 最近30分钟发生过什么` | `event_window` |
| `FortiGate 如何配置 SD-WAN 健康检查` | `official_knowledge + operational_memory` |
| `192.168.1.23 最近30分钟为什么断网` | `direct_state + event_window + operational_memory + official_knowledge` |

IP、时间范围和查询源由代码绑定。模型接收已经限定范围、带来源和时间戳的证据，负责归纳、解释和生成后续调查建议。

### 3. 当前状态快路径

- 单设备当前状态使用窄设备 API 读取。
- 该路径不读取 ClickHouse，不检索官方手册，不运行全量网络探针。
- 本次线上请求实测约 5.0 秒，只生成 1 条设备直读证据。
- 更复杂的根因请求会并发读取设备当前状态和历史事件。

原始 API 在单设备状态查询上拥有最低延迟。调查服务的价值集中在跨时间、跨设备、跨知识源的证据关联、留存和复盘。

### 4. ClickHouse 历史事实链路

- 生产摄取写入 `autopoiesis.facts_v2`。
- 已修复多个读取端仍查询旧表 `autopoiesis.facts` 的问题。
- 调查请求绕过 120 秒页面缓存，直接读取最新可查询事实。
- 历史窗口在请求时计算，支持精确 `windowStart/windowEnd`。
- “最近30分钟”已经在线验证为精确 30 分钟查询。
- 精确窗口无数据时直接返回空结果，不扩大到 7 天或 90 天。
- 响应同时区分设备静默时长、全局摄取链路滞后和事件写入延迟。
- 本次样本中，全局摄取链路滞后约 1.5 至 3.5 秒，单条事件写入延迟样本为 449 ms。这些数值是点时测量结果，后续仍需形成连续 SLO。

直接设备 API 提供查询时刻的控制面和会话快照。ClickHouse 提供可重放的时间窗口、计数、序列、首次和末次观测以及跨设备聚合。

### 5. BGE、FAISS、RRF 的当前决策

线上默认路径为 BM25，以下组件当前关闭：

- BGE：文本向量模型。
- sentence-transformers 和 Torch：本地运行 BGE 的依赖栈。
- FAISS Flat/HNSW：向量索引。
- RRF：BM25 与向量结果的排名融合算法。

部署进程的内存映射已经确认没有加载 `torch`、`sentence_transformers` 和 `faiss`。网关依赖文件与容器构建也取消了强制安装和预下载。

可选向量能力保留用于后续受控评测。启用条件是固定查询集上的 Recall、nDCG 或根因证据命中率产生稳定增益，并同时满足延迟和内存预算。

索引选择已实现自动策略：

- 向量规模不超过 10 万时选择 Flat。
- 超过 10 万时按目标 Recall 配置 HNSW 搜索深度。
- 当前基准中，100 万向量、Recall@10 约 0.96 时，HNSW 的 p95 延迟约为 Flat 的 51%。目标提高到约 0.99 时，HNSW 延迟会超过 Flat。

当前知识库只有 6384 个高度版本化切片，BM25 已覆盖设备名、命令、配置项和错误码等强关键词场景，因此线上没有启用向量链路。

### 6. 故障记忆和上下文质量

- 记忆写入包含症状、证据、根因、处理动作、验证结果、实体和适用版本。
- 缺少关键证据、内容过浅、只有模板槽位的记录无法成为高置信复用记忆。
- 检索先按实体、版本、状态和时间过滤，再进入文本匹配。
- 上下文根据当前问题选择证据，限制重复项和单项长度，同时保留证据 ID、来源、观测时间和成功状态。
- 版本不匹配的官方知识和故障经验会降级或排除。

## 当前业务价值边界

已落地并有技术验证的价值：

1. 当前状态查询减少无关历史、文档和探针开销。
2. 历史调查使用准确数据表和准确时间窗口，降低错取证据的风险。
3. 官方手册自动匹配设备版本，减少人工准备语料和引用错误版本的概率。
4. 每条结论能够追溯到设备读取、历史查询、官方知识或已验证记忆。
5. 简单查询只使用必要数据源，控制模型上下文和 Token 消耗。
6. 根因调查能够同时比较当前状态与历史变化，支持复盘和证据留存。

尚未完成的业务证明：

- 缺少真实事故对照实验，当前不能给出 MTTR 降低比例。
- 缺少固定事故集上的端到端根因准确率、误报率和证据完整率。
- 缺少其他主流网络厂商的自动知识提供器。
- 缺少持续采样形成的设备 API 延迟、ClickHouse 摄取滞后和完整调查耗时 SLO。

## 已完成验证

- 主测试集：`1388 passed, 24 skipped`。
- 生产写入隔离测试：停服务后单独执行，`2 passed`。
- 合计：`1390 passed, 24 skipped`。
- 网关服务已恢复，`/api/healthz` 返回 `status=ok`。
- 官方知识查询“FortiGate 如何配置 SD-WAN 健康检查”实测 0.21 秒，命中 4 个上下文章节，没有执行设备探针。
- 当前状态查询实测约 5.0 秒，只读取直接状态源。
- 精确 30 分钟根因查询已验证 ClickHouse 命令中携带完整起止时间。

## 关键实现入口

- `core/memory/knowledge_sync.py`：设备指纹、官方知识同步、覆盖率门控和原子发布。
- `core/memory/ops_knowledge.py`：线上知识库装载、BM25 与可选混合检索路由。
- `core/memory/quality.py`：故障记忆质量门控。
- `core/eval/dense_retrieval.py`：Flat/HNSW 自动选择和搜索参数。
- `domains/network_rca/query_contract.py`：自然语言实体、时间范围、意图和数据源计划。
- `domains/network_rca/fortigate_api.py`：FortiGate 身份与状态读取。
- `frontend/gateway/app/investigation_tools.py`：设备主体解析和实时采集适配器。
- `frontend/gateway/app/history.py`：ClickHouse 历史窗口和时效字段。
- `frontend/gateway/app/investigate.py`：多源并发、证据账本和调查会话。
- `frontend/gateway/app/main.py`：知识状态与调查 HTTP 接口。

## 建议手机端继续讨论的问题

1. 选取 20 至 50 个真实事故，怎样定义根因正确、证据完整和无效探针三个评价指标？
2. 哪些设备厂商和型号覆盖业务风险最高，应该成为下一批官方知识提供器？
3. 当前状态接口怎样进一步减少设备侧串行调用，将约 5 秒压缩到目标范围？
4. 哪些自然语言问题确实造成 BM25 漏召回，是否足以证明 BGE + RRF 的收益？
5. 故障记忆需要达到哪些证据字段和验证条件，才能参与后续自动建议？
6. 端到端调查需要建立哪些 SLO：数据新鲜度、首屏时间、完整调查时间、证据覆盖率和失败降级率？

## Git 状态说明

- 本进度文件将单独提交到 GitHub `main`。
- 实现改动当前仍在部署机器工作区，包含后端、前端和测试文件。
- 后续应先审查工作区改动归属，再把实现拆成可审查的提交；避免把既有用户改动和本轮 RCA 改动混为一个提交。
