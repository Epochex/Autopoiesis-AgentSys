{
  "name": "selfevo-orchiter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript-first self-evolving agent orchestration platform for enterprise memory, context compression, and policy iteration.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "harness:gate": "npm run build && node dist/src/cli/harnessGate.js",
    "harness:replay": "npm run build && node dist/src/cli/harnessReplay.js",
    "provider:smoke": "npm run build && node dist/src/cli/providerSmoke.js",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "test:provider": "npm run build && SELFEVO_RUN_PROVIDER_SMOKE=1 node --test dist/tests/providerSmoke.live.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.15.24",
    "typescript": "^5.8.3"
  }
}
